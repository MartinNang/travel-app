export default class User {
  constructor(email, password, profileName, profileImage) {
    this.email = email;
    this.password = password;
    this.profileName = profileName;
    this.profileImage = profileImage;
  }
}
