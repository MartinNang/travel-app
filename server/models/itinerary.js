export default class Itinerary {
  constructor(name, userId, startDate, endDate, type) {
    this.name = name;
    this.userId = userId;
    this.startDate = startDate;
    this.endDate = endDate;
    this.type = type;
  }
}
